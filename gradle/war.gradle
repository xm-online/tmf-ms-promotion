apply plugin: "war"

bootWar {
    mainClass = "com.icthh.xm.tmf.ms.promotion.PromotionApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
